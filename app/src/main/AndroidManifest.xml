<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.e.canhalalres">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CanHalalRes"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activity.TrackOrder"></activity>
        <activity android:name=".Activity.AddToCart" />
        <activity
            android:name=".Activity.Login"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:windowSoftInputMode="adjustPan">

        </activity>
        <activity android:name=".Activity.ResetPassword" />
        <activity android:name=".Activity.OTPActivity" />
        <activity android:name=".Activity.ForgetPassword" />
        <activity android:name=".Activity.Registration" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Activity.Dashboard.Dashboard" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>